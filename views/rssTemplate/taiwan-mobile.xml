{% extends "./base/taiwanMobileBase.xml" %}

{% block item %}
    {% for item in items %}
        <item>
            <id>{{item.id}}</id>
            <title>{{ item.title }}</title>
            <link>{{ item.url }}</link>
            <description>
                <![CDATA[
                    {{ item.mainPhotoBody | safe }}%THISISAD%{{ item.description | safe }}
                ]]>
            </description>
            <summary>
                {{ item.summary }}
            </summary>
            <guid isPermaLink="true">{{ item.url }}</guid>
            <subcategory>
                <![CDATA[
                    {{ item.subcategory }}
                ]]>
            </subcategory>
            <author>{{ item.author }}</author>
            <pubDate>{{ item.UTCdate }}</pubDate>
        </item>
    {% endfor %}
{% endblock %}
